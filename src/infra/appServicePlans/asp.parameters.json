{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "app": {
      "value": {
        "name": "#{{ environment }}-#{{ projectName }}-#{{ nc_appserviceplan }}-#{{ region }}-01",
        "skuTier": "#{{ skuTier }}",
        "skuCode": "#{{ skuCode }}",
        "skuCapacity": "#{{ skuCapacity }}",
        "skuFamily": "#{{ skuFamily }}"
      }
    },
    "defaultTags": {
      "value": {}
    }
  }
}